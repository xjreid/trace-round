# Local Vite uses its proxy, so these defaults can remain unchanged.
VITE_API_BASE_URL=/api
VITE_BACKEND_ORIGIN=

# Production example:
# VITE_API_BASE_URL=https://api.example.com/api
# VITE_BACKEND_ORIGIN=https://api.example.com
